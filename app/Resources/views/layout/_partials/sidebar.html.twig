<div class="sidebar sidebar-main sidebar-fixed">
    <div class="sidebar-content">

        <!-- User menu -->
        <div class="sidebar-user">
            <div class="category-content">
                <div class="media">
                    <a href="{{ path('fos_user_profile_show') }}" class="media-left"><img src="{{ asset('assets/images/avatar.png') }}" class="img-circle img-sm" alt=""></a>
                    <div class="media-body">
                        <span class="media-heading text-semibold">{{ app.user.firstName|default('') }} {{ app.user.lastName|default('') }}</span>
                        <div class="text-size-mini text-muted">
                            <i class="icon-pin text-size-small"></i> {{ app.user.city|default('') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- /user menu -->


        <!-- Main navigation -->
        <div class="sidebar-category sidebar-category-visible">
            <div class="category-content no-padding">
                {{ knp_menu_render('main', {'allow_safe_labels': true, 'currentClass': 'current active'}) }}

                <ul class="navigation navigation-main navigation-accordion">

                    <!-- Main -->
                    <!-- /main -->

                </ul>
            </div>
        </div>
        <!-- /main navigation -->

    </div>
</div>

