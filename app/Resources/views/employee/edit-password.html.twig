{% extends 'employee/employee.html.twig' %}
{% form_theme edit_form 'Form/fields.html.twig' %}

{% block _content %}

    <div class="panel panel-flat">
        <div class="panel-heading">
            <h6 class="panel-title">Employee edit<a class="heading-elements-toggle"><i class="icon-more"></i></a></h6>
            <div class="heading-elements">
                <a href="{{ path('employee_index') }}" class="btn btn-primary btn-xs "><b><i class="icon-loop3"></i></b> Retour à la liste</a>
            </div>
        </div>
        <div class="panel-body">
            <p class="content-group-lg"></p>

            {{ form_start(edit_form, {'attr': {'class': 'form-horizontal'}}) }}
            {{ form_widget(edit_form) }}
            <div class="text-right">
                <div class="btn-group">
                    <button class="btn btn-primary legitRipple" type="submit">Modifier<i class="icon-rotate-cw2 position-right"></i></button>
                </div>
            </div>
            {{ form_end(edit_form) }}
        </div>
    </div>
{% endblock %}
